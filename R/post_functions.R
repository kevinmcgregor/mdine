
#' Get adjacency matrix for estimated networks based on credible intervals
#'
#' @param obj An object of 'mdine' class
#' @param weighted Logical: should
#'
#' @return Adjacency matrices for
#' @export
#'
#' @examples
ci_to_adj <- function(obj, weighted=FALSE) {
  # if (class(obj) != "mdine") stop("obj must be of class \"mdine\"")
  # upper0 <-
  # adj <- (lower>0) | (upper<0)
  # diag(adj) <- 0
  # return(adj)
  return(1)
}

